---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/validators/tsconfig.json
  - packages/validators/src/index.ts
  - packages/validators/src/product.ts
  - packages/validators/src/lead.ts
  - packages/validators/src/quotation.ts
  - packages/validators/src/order.ts
  - packages/validators/src/payment.ts
  - packages/validators/src/kpi.ts
  - packages/validators/src/warehouse.ts
  - packages/validators/src/user.ts
  - packages/validators/src/stockItem.ts
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/db/src/mock/index.ts
  - packages/db/src/mock/products.ts
  - packages/db/src/mock/leads.ts
  - packages/db/src/mock/quotations.ts
  - packages/db/src/mock/orders.ts
  - packages/db/src/mock/payments.ts
  - packages/db/src/mock/kpis.ts
  - packages/db/src/mock/warehouses.ts
  - packages/db/src/mock/users.ts
autonomous: true
requirements:
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "Importing `ProductSchema` from `@furnitrack/validators` and calling `.parse({})` on it produces a typed Zod error — TypeScript resolves the type"
    - "All 8 domain entity schemas exist and export both the schema object and its inferred TypeScript type"
    - "`MOCK_PRODUCTS` from `@furnitrack/db/mock` is typed as `Product[]` — TypeScript rejects adding a product with a missing required field"
    - "Mock data covers all downstream screens: 10–12 products across 6 categories, 5–8 leads across all CRM stages, 5–8 quotations, 3–5 orders with lineage references, 3–5 payments, 1 KPI object, 2–3 warehouses, 5 users (one per role)"
  artifacts:
    - path: "packages/validators/src/product.ts"
      provides: "ProductSchema, Product type, StockStatusEnum, ProductBadgeEnum"
      exports: ["ProductSchema", "Product", "StockStatusEnum", "StockStatus", "ProductBadgeEnum", "ProductBadge"]
    - path: "packages/validators/src/index.ts"
      provides: "Barrel export of all 8 entity schemas"
      contains: "export * from"
    - path: "packages/db/src/mock/products.ts"
      provides: "MOCK_PRODUCTS typed as Product[]"
      contains: "MOCK_PRODUCTS"
    - path: "packages/db/src/mock/index.ts"
      provides: "@furnitrack/db/mock barrel export"
      contains: "MOCK_PRODUCTS"
  key_links:
    - from: "packages/db/src/mock/products.ts"
      to: "packages/validators/src/product.ts"
      via: "import type { Product } from @furnitrack/validators"
      pattern: "import.*Product.*@furnitrack/validators"
    - from: "packages/db/src/mock/index.ts"
      to: "packages/db/src/mock/*.ts"
      via: "barrel re-exports"
      pattern: "export.*from.*\\./"
---

<objective>
Implement `packages/validators` (all 8 Zod entity schemas) and `packages/db/src/mock/` (all fixture data files), establishing the type-safe data layer that every downstream phase imports from.

Purpose: These two packages are the single source of truth for data shapes. Every phase 2–6 component receives its props typed against these schemas. Getting the shapes right now prevents type debt in every subsequent plan.

Output: 8 Zod schemas with inferred TypeScript types in `@furnitrack/validators`, and typed mock fixture arrays in `@furnitrack/db/mock` covering all domain entities.
</objective>

<execution_context>
@C:/Users/bowie ramirez/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bowie ramirez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md

<interfaces>
<!-- From packages/config — already created in Plan 01 -->
<!-- packages/config/typescript/base.json exports strict TS config -->
<!-- packages/validators/package.json and packages/db/package.json already exist with correct exports map -->
<!-- packages/validators/src/index.ts currently has `export {}` placeholder — REPLACE entirely -->
<!-- packages/db/src/mock/index.ts currently has `export {}` placeholder — REPLACE entirely -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all 8 Zod schemas in packages/validators</name>
  <files>
    packages/validators/tsconfig.json,
    packages/validators/src/product.ts,
    packages/validators/src/user.ts,
    packages/validators/src/lead.ts,
    packages/validators/src/quotation.ts,
    packages/validators/src/order.ts,
    packages/validators/src/payment.ts,
    packages/validators/src/kpi.ts,
    packages/validators/src/warehouse.ts,
    packages/validators/src/stockItem.ts,
    packages/validators/src/index.ts
  </files>
  <action>
    First, install the zod dependency and add tsconfig:

    ```bash
    pnpm add zod@^3 --filter @furnitrack/validators
    ```

    **`packages/validators/tsconfig.json`**:
    ```json
    {
      "extends": "@furnitrack/config/typescript/base.json",
      "compilerOptions": {
        "outDir": "dist"
      },
      "include": ["src/**/*.ts"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    Create one file per entity. All schemas follow the pattern: `z.object(...)` → `export const Schema`, `export type Type = z.infer<typeof Schema>`.

    **`packages/validators/src/product.ts`** — exact shape from CONTEXT.md:
    ```typescript
    import { z } from "zod"

    export const StockStatusEnum = z.enum(["IN_STOCK", "LOW_STOCK", "OUT_OF_STOCK"])
    export const ProductBadgeEnum = z.enum(["BEST_SELLER", "SALE", "HOT"]).nullable()

    export const ColorVariantSchema = z.object({
      name: z.string(),
      hex: z.string().regex(/^#[0-9a-fA-F]{6}$/),
    })

    export const DimensionsSchema = z.object({
      width: z.number().positive(),
      depth: z.number().positive(),
      height: z.number().positive(),
      weight: z.number().positive(),
    })

    export const ProductSchema = z.object({
      id: z.string(),
      slug: z.string(),
      name: z.string(),
      category: z.string(),
      material: z.string(),
      price: z.number().positive(),
      originalPrice: z.number().positive().nullable(),
      badge: ProductBadgeEnum,
      stockStatus: StockStatusEnum,
      colorVariants: z.array(ColorVariantSchema),
      images: z.array(z.string()),
      rating: z.number().min(0).max(5),
      reviewCount: z.number().int().nonnegative(),
      description: z.string(),
      dimensions: DimensionsSchema,
    })

    export type Product = z.infer<typeof ProductSchema>
    export type StockStatus = z.infer<typeof StockStatusEnum>
    export type ProductBadge = z.infer<typeof ProductBadgeEnum>
    export type ColorVariant = z.infer<typeof ColorVariantSchema>
    export type Dimensions = z.infer<typeof DimensionsSchema>
    ```

    **`packages/validators/src/user.ts`**:
    ```typescript
    import { z } from "zod"

    export const RoleEnum = z.enum(["MANAGEMENT", "SALES", "ACCOUNTING", "INVENTORY", "ADMIN"])

    export const UserSchema = z.object({
      id: z.string(),
      name: z.string(),
      email: z.string().email(),
      role: RoleEnum,
      branchId: z.string(),
    })

    export type User = z.infer<typeof UserSchema>
    export type Role = z.infer<typeof RoleEnum>
    ```

    **`packages/validators/src/lead.ts`**:
    ```typescript
    import { z } from "zod"

    export const CrmStageEnum = z.enum(["NEW", "QUOTED", "NEGOTIATING", "CONFIRMED", "CLOSED"])
    export const LeadStatusEnum = z.enum(["ACTIVE", "INACTIVE", "LOST"])

    export const LeadSchema = z.object({
      id: z.string(),
      name: z.string(),
      company: z.string(),
      email: z.string().email(),
      businessType: z.string(),
      budget: z.number().positive(),
      targetDate: z.string(),
      stage: CrmStageEnum,
      status: LeadStatusEnum,
      createdAt: z.string(),
    })

    export type Lead = z.infer<typeof LeadSchema>
    export type CrmStage = z.infer<typeof CrmStageEnum>
    ```

    **`packages/validators/src/quotation.ts`**:
    ```typescript
    import { z } from "zod"

    export const QuotationStatusEnum = z.enum(["DRAFT", "SENT", "IN_REVIEW", "CONFIRMED", "REJECTED"])

    export const QuotationLineItemSchema = z.object({
      productId: z.string(),
      productName: z.string(),
      quantity: z.number().int().positive(),
      unitPrice: z.number().positive(),
      dimensions: z.string().optional(),
      finish: z.string().optional(),
      total: z.number().positive(),
    })

    export const QuotationSchema = z.object({
      id: z.string(),
      quoteNumber: z.string(),
      leadId: z.string(),
      clientName: z.string(),
      clientEmail: z.string().email(),
      status: QuotationStatusEnum,
      lineItems: z.array(QuotationLineItemSchema),
      subtotal: z.number().nonnegative(),
      vatAmount: z.number().nonnegative(),
      total: z.number().nonnegative(),
      orderId: z.string().nullable(),
      createdAt: z.string(),
      updatedAt: z.string(),
    })

    export type Quotation = z.infer<typeof QuotationSchema>
    export type QuotationStatus = z.infer<typeof QuotationStatusEnum>
    export type QuotationLineItem = z.infer<typeof QuotationLineItemSchema>
    ```

    **`packages/validators/src/order.ts`**:
    ```typescript
    import { z } from "zod"

    export const OrderStatusEnum = z.enum([
      "PENDING",
      "CONFIRMED",
      "IN_PRODUCTION",
      "DELIVERED",
      "CANCELLED",
    ])

    export const OrderSchema = z.object({
      id: z.string(),
      soNumber: z.string(),
      poNumber: z.string().nullable(),
      quotationId: z.string(),
      clientName: z.string(),
      status: OrderStatusEnum,
      rushOrder: z.boolean(),
      deliveryDate: z.string(),
      subtotal: z.number().nonnegative(),
      vatAmount: z.number().nonnegative(),
      total: z.number().nonnegative(),
      createdAt: z.string(),
    })

    export type Order = z.infer<typeof OrderSchema>
    export type OrderStatus = z.infer<typeof OrderStatusEnum>
    ```

    **`packages/validators/src/payment.ts`**:
    ```typescript
    import { z } from "zod"

    export const PaymentTypeEnum = z.enum(["DOWN_PAYMENT", "PARTIAL", "FULL"])
    export const PaymentStatusEnum = z.enum(["PENDING", "CONFIRMED", "REJECTED"])

    export const PaymentSchema = z.object({
      id: z.string(),
      orderId: z.string(),
      type: PaymentTypeEnum,
      amount: z.number().positive(),
      status: PaymentStatusEnum,
      proofUrl: z.string().nullable(),
      date: z.string(),
    })

    export type Payment = z.infer<typeof PaymentSchema>
    export type PaymentType = z.infer<typeof PaymentTypeEnum>
    export type PaymentStatus = z.infer<typeof PaymentStatusEnum>
    ```

    **`packages/validators/src/kpi.ts`** — shape from CONTEXT.md (exact fields required):
    ```typescript
    import { z } from "zod"

    export const KpiSchema = z.object({
      dailySales: z.number(),
      monthlySales: z.number(),
      revenue: z.number(),
      profitMargin: z.number(),
      activeOrders: z.number().int(),
      pendingOrders: z.number().int(),
      outstandingReceivables: z.number(),
      supplierLeadTimeDays: z.number(),
      lowStockAlertCount: z.number().int(),
      stockTurnover: z.number(),
      revenueChange: z.number(),
      salesChange: z.number(),
      profitChange: z.number(),
      ordersChange: z.number(),
    })

    export type Kpi = z.infer<typeof KpiSchema>
    ```

    **`packages/validators/src/warehouse.ts`**:
    ```typescript
    import { z } from "zod"

    export const WarehouseSchema = z.object({
      id: z.string(),
      name: z.string(),
      branchCode: z.string(),
      address: z.string(),
    })

    export type Warehouse = z.infer<typeof WarehouseSchema>
    ```

    **`packages/validators/src/stockItem.ts`**:
    ```typescript
    import { z } from "zod"

    export const StockStateEnum = z.enum(["AVAILABLE", "RESERVED", "IN_PRODUCTION", "DELIVERED"])
    export const StockMovementTypeEnum = z.enum([
      "IN",
      "OUT",
      "TRANSFER",
      "ADJUSTMENT",
      "DAMAGED",
    ])

    export const StockItemSchema = z.object({
      id: z.string(),
      productId: z.string(),
      productName: z.string(),
      sku: z.string(),
      warehouseId: z.string(),
      warehouseName: z.string(),
      qtyAvailable: z.number().int().nonnegative(),
      qtyReserved: z.number().int().nonnegative(),
      qtyInProduction: z.number().int().nonnegative(),
      minThreshold: z.number().int().nonnegative(),
      state: StockStateEnum,
    })

    export type StockItem = z.infer<typeof StockItemSchema>
    export type StockState = z.infer<typeof StockStateEnum>
    export type StockMovementType = z.infer<typeof StockMovementTypeEnum>
    ```

    **`packages/validators/src/index.ts`** — barrel (replaces `export {}` placeholder):
    ```typescript
    export * from "./product"
    export * from "./user"
    export * from "./lead"
    export * from "./quotation"
    export * from "./order"
    export * from "./payment"
    export * from "./kpi"
    export * from "./warehouse"
    export * from "./stockItem"
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem" &amp;&amp; pnpm --filter @furnitrack/validators typecheck 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>`pnpm --filter @furnitrack/validators typecheck` exits 0 with no TypeScript errors. All 9 source files exist in `packages/validators/src/`.</done>
</task>

<task type="auto">
  <name>Task 2: Create all mock fixture files in packages/db/src/mock/</name>
  <files>
    packages/db/tsconfig.json,
    packages/db/src/index.ts,
    packages/db/src/mock/products.ts,
    packages/db/src/mock/leads.ts,
    packages/db/src/mock/quotations.ts,
    packages/db/src/mock/orders.ts,
    packages/db/src/mock/payments.ts,
    packages/db/src/mock/kpis.ts,
    packages/db/src/mock/warehouses.ts,
    packages/db/src/mock/users.ts,
    packages/db/src/mock/index.ts
  </files>
  <action>
    Install `@furnitrack/validators` as a workspace dep for `packages/db`, add tsconfig, then write all fixture files.

    ```bash
    pnpm add @furnitrack/validators@workspace:* --filter @furnitrack/db
    ```

    **`packages/db/tsconfig.json`**:
    ```json
    {
      "extends": "@furnitrack/config/typescript/base.json",
      "compilerOptions": {
        "outDir": "dist"
      },
      "include": ["src/**/*.ts"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    **`packages/db/src/index.ts`** — top-level package export (not mock):
    ```typescript
    // Phase 1: No Prisma client — DB package exports mock data only for MVP milestone
    // Real Prisma client will be added in the backend milestone
    export * from "./mock"
    ```

    **`packages/db/src/mock/products.ts`** — 12 products covering all 6 categories (Living Room, Bedroom, Dining, Office, Storage, Lighting). Each product must have all required fields. Use Philippine peso pricing (₱ 3,500–₱ 45,000 range). Use placeholder image paths like `/images/products/{slug}.jpg`:

    ```typescript
    import type { Product } from "@furnitrack/validators"

    export const MOCK_PRODUCTS: Product[] = [
      {
        id: "prod-001",
        slug: "ariston-oak-dining-chair",
        name: "Ariston Oak Dining Chair",
        category: "Dining",
        material: "Solid Oak",
        price: 4500,
        originalPrice: 5200,
        badge: "SALE",
        stockStatus: "IN_STOCK",
        colorVariants: [
          { name: "Natural Oak", hex: "#c9a84c" },
          { name: "Dark Walnut", hex: "#3d2b1f" },
        ],
        images: ["/images/products/ariston-oak-dining-chair.jpg"],
        rating: 4.8,
        reviewCount: 127,
        description: "Solid oak dining chair with ergonomic curved back and upholstered seat. Kiln-dried hardwood frame ensures lasting durability.",
        dimensions: { width: 45, depth: 52, height: 90, weight: 6.5 },
      },
      // ... create 11 more products:
      // prod-002: Living Room sofa (BEST_SELLER, IN_STOCK, Fabric/Wood)
      // prod-003: Bedroom platform bed (HOT, IN_STOCK, Engineered Wood)
      // prod-004: Office ergonomic desk (null badge, IN_STOCK, Steel/Wood)
      // prod-005: Storage cabinet (SALE, LOW_STOCK, MDF)
      // prod-006: Pendant ceiling lamp (BEST_SELLER, IN_STOCK, Metal/Glass)
      // prod-007: Living Room coffee table (null badge, LOW_STOCK, Tempered Glass/Steel)
      // prod-008: Bedroom wardrobe (null badge, IN_STOCK, Solid Wood)
      // prod-009: Dining table set 6-seater (HOT, IN_STOCK, Mahogany)
      // prod-010: Office bookshelf (null badge, OUT_OF_STOCK, Engineered Wood)
      // prod-011: Bedroom bedside table (SALE, IN_STOCK, Solid Wood)
      // prod-012: Storage shoe rack (null badge, IN_STOCK, Metal)
      // Ensure stockStatus mix: 8 IN_STOCK, 2 LOW_STOCK, 1 OUT_OF_STOCK — varies for realistic filter sidebar content
    ]
    ```

    **`packages/db/src/mock/leads.ts`** — 7 leads, spread across all 5 CRM stages:
    ```typescript
    import type { Lead } from "@furnitrack/validators"

    export const MOCK_LEADS: Lead[] = [
      // NEW: 2 leads
      // QUOTED: 1 lead
      // NEGOTIATING: 1 lead
      // CONFIRMED: 2 leads
      // CLOSED: 1 lead
      // Fields: id, name, company, email, businessType (Hotel/Restaurant/Office/Residential), budget, targetDate (ISO date string), stage, status (ACTIVE/INACTIVE/LOST), createdAt
    ]
    ```

    **`packages/db/src/mock/quotations.ts`** — 7 quotations, mix of statuses, some with `orderId` linking to an order:
    ```typescript
    import type { Quotation } from "@furnitrack/validators"

    export const MOCK_QUOTATIONS: Quotation[] = [
      // Include quotations in: DRAFT (1), SENT (2), IN_REVIEW (1), CONFIRMED (2), REJECTED (1)
      // At least 2 confirmed quotations must have orderId pointing to an order in MOCK_ORDERS
      // Each quotation must have 2–4 line items with productId, productName, quantity, unitPrice, total
      // subtotal, vatAmount (12% VAT), total computed correctly
    ]
    ```

    **`packages/db/src/mock/orders.ts`** — 4 orders, each referencing a quotation:
    ```typescript
    import type { Order } from "@furnitrack/validators"

    export const MOCK_ORDERS: Order[] = [
      // Statuses: PENDING (1), CONFIRMED (1), IN_PRODUCTION (1), DELIVERED (1)
      // Each order has quotationId linking back to a confirmed quotation
      // At least 1 rushOrder: true
      // soNumber format: "SO-2026-001", poNumber: "PO-2026-001" or null
    ]
    ```

    **`packages/db/src/mock/payments.ts`** — 4 payments across the 4 orders:
    ```typescript
    import type { Payment } from "@furnitrack/validators"

    export const MOCK_PAYMENTS: Payment[] = [
      // Mix: DOWN_PAYMENT (2), PARTIAL (1), FULL (1)
      // Statuses: CONFIRMED (2), PENDING (2)
      // proofUrl: "/uploads/proof-{id}.jpg" or null
    ]
    ```

    **`packages/db/src/mock/kpis.ts`** — exactly 1 KPI object (all MGMT dashboard fields):
    ```typescript
    import type { Kpi } from "@furnitrack/validators"

    export const MOCK_KPIS: Kpi = {
      dailySales: 125000,
      monthlySales: 2840000,
      revenue: 18500000,
      profitMargin: 32.5,
      activeOrders: 23,
      pendingOrders: 7,
      outstandingReceivables: 4200000,
      supplierLeadTimeDays: 14,
      lowStockAlertCount: 3,
      stockTurnover: 4.2,
      revenueChange: 12.3,
      salesChange: 8.7,
      profitChange: -2.1,
      ordersChange: 15.4,
    }
    ```

    **`packages/db/src/mock/warehouses.ts`** — 3 warehouses:
    ```typescript
    import type { Warehouse } from "@furnitrack/validators"

    export const MOCK_WAREHOUSES: Warehouse[] = [
      { id: "wh-001", name: "Main Warehouse", branchCode: "MNL", address: "123 Industrial Ave, Manila" },
      { id: "wh-002", name: "Cebu Branch", branchCode: "CEB", address: "45 Pier Road, Cebu City" },
      { id: "wh-003", name: "Davao Branch", branchCode: "DAV", address: "78 Warehouse St, Davao City" },
    ]
    ```

    **`packages/db/src/mock/users.ts`** — exactly 5 users, one per role:
    ```typescript
    import type { User } from "@furnitrack/validators"

    export const DEMO_USERS: User[] = [
      { id: "user-001", name: "Karen Alonzo", email: "karen@furnitrack.com", role: "MANAGEMENT", branchId: "wh-001" },
      { id: "user-002", name: "Genie Rose Gonzales", email: "genie@furnitrack.com", role: "SALES", branchId: "wh-001" },
      { id: "user-003", name: "Maria Santos", email: "maria@furnitrack.com", role: "ACCOUNTING", branchId: "wh-001" },
      { id: "user-004", name: "Juan Reyes", email: "juan@furnitrack.com", role: "INVENTORY", branchId: "wh-001" },
      { id: "user-005", name: "Admin User", email: "admin@furnitrack.com", role: "ADMIN", branchId: "wh-001" },
    ]
    ```

    **`packages/db/src/mock/index.ts`** — barrel (replaces `export {}` placeholder):
    ```typescript
    export { MOCK_PRODUCTS } from "./products"
    export { MOCK_LEADS } from "./leads"
    export { MOCK_QUOTATIONS } from "./quotations"
    export { MOCK_ORDERS } from "./orders"
    export { MOCK_PAYMENTS } from "./payments"
    export { MOCK_KPIS } from "./kpis"
    export { MOCK_WAREHOUSES } from "./warehouses"
    export { DEMO_USERS } from "./users"
    ```

    CRITICAL: All fixture files import types using `import type { X } from "@furnitrack/validators"` — not values, just types. This prevents circular dependency risks and keeps the mock package as a pure data package.

    Do NOT use `z.parse()` or `z.safeParse()` in fixture files — types only, no runtime Zod validation in fixtures.
  </action>
  <verify>
    <automated>cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem" &amp;&amp; pnpm --filter @furnitrack/db typecheck 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>`pnpm --filter @furnitrack/db typecheck` exits 0. All 8 mock fixture files exist in `packages/db/src/mock/`. `MOCK_PRODUCTS` array has exactly 12 entries. `MOCK_KPIS` is a single object (not an array).</done>
</task>

</tasks>

<verification>
Run `pnpm turbo typecheck --filter=@furnitrack/validators --filter=@furnitrack/db` from repo root. Both packages should typecheck cleanly. Check that `MOCK_PRODUCTS[0].stockStatus` produces the correct TypeScript type `"IN_STOCK" | "LOW_STOCK" | "OUT_OF_STOCK"` — verifiable by the typecheck passing without errors on the typed fixture arrays.
</verification>

<success_criteria>
- `packages/validators/src/` contains 9 files (8 entity schemas + index.ts)
- `packages/db/src/mock/` contains 9 files (8 fixture files + index.ts)
- `pnpm --filter @furnitrack/validators typecheck` exits 0
- `pnpm --filter @furnitrack/db typecheck` exits 0
- MOCK_PRODUCTS: 12 entries, all fields present, 6 categories represented
- MOCK_LEADS: 7 entries spread across all 5 CRM stages
- MOCK_KPIS: single object (not array) with all 14 fields
- FOUN-03: Zod schemas for all domain entities exported — confirmed
- FOUN-04: All typed mock fixture files in packages/db/src/mock/ — confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md` using the standard summary template.
</output>
