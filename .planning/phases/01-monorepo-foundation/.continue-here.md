---
phase: 01-monorepo-foundation
task: storefront-pages
total_tasks: 3
status: in_progress
last_updated: "2026-03-06T16:05:14.209Z"
---

<current_state>
Phase 1 plans 01-01, 01-02, 01-03 are complete with SUMMARYs. Plan 01-04 is mid-execution:
- automated Tasks 1 & 2 done (Prisma schema, both apps scaffolded, design-system smoke-test pages)
- checkpoint (Task 3) is STILL OPEN — awaiting human visual verification of /ui-preview page
- SEPARATELY from the GSD plan, the user asked to build the full storefront UI matching 3 reference designs

Two parallel threads:
1. GSD Phase 1 checkpoint still needs "approved" from user (visual smoke test at http://localhost:3010/ui-preview)
2. Storefront UI build — homepage done, shop + product detail pages not yet written
</current_state>

<completed_work>

## GSD Phase 1 Plans
- Plan 01-01: Monorepo bootstrap + packages/config — COMPLETE (SUMMARY exists)
- Plan 01-02: Zod validators + mock fixtures — COMPLETE (SUMMARY exists)
- Plan 01-03: shadcn/ui setup + 4 UI components — COMPLETE (SUMMARY exists)
- Plan 01-04: Apps scaffold + Prisma schema — automated tasks done, CHECKPOINT OPEN

## Storefront UI (outside GSD plan — user-requested)
- Fixed Tailwind v4 styling: added `@source` directive + shadcn CSS variables to both apps globals.css
- Created `~/.claude/skills/frontend-design.md` — pixel-perfect frontend design skill
- Built `apps/storefront/components/Navbar.tsx` — navy header, search, logo, nav links
- Built `apps/storefront/components/Footer.tsx` — navy footer, 4-col links, social icons
- Built `apps/storefront/components/ProductCard.tsx` — image, badge, price, rating, hover
- Built `apps/storefront/app/page.tsx` — FULL homepage:
  - Hero: 2-col with 2×2 card grid (main card left, 3 promo cards right)
  - Trust bar: 4 icons (Free Shipping, Warranty, Returns, Support) on beige bg
  - New Arrivals section: 4-col product grid
  - Mid-page CTA block: navy bg, gold accent, image right
  - Curated For You section: 4-col product grid
  - Trending Now section: 4-col product grid
  - Bottom CTA banner: dark overlay with "Unlock the Full Experience"
- Renamed `design-system` route → `ui-preview` in both storefront and admin apps

## Uncommitted files
- apps/storefront/components/ (Navbar, Footer, ProductCard — NEW)
- apps/storefront/app/page.tsx (MODIFIED — full homepage)
- apps/storefront/app/ui-preview/ (RENAMED from design-system)
- apps/admin/app/ui-preview/ (RENAMED from design-system)
- apps/storefront/app/globals.css (MODIFIED — Tailwind v4 fix)
- apps/admin/app/globals.css (MODIFIED — Tailwind v4 fix)
- apps/admin/app/page.tsx (MODIFIED — updated /design-system link to /ui-preview)
- README.md (MODIFIED — full project README)
</completed_work>

<remaining_work>

## Storefront pages still to build
1. **Shop page** — `apps/storefront/app/shop/page.tsx`
   - Hero banner (full-width with "Simple is More" italic heading)
   - Breadcrumb: Home > Furniture
   - Sort bar + result count ("13 results for furniture")
   - 2-col layout: filter sidebar left (Category checkboxes w/ counts, Material, Price slider, Color swatches) + 3-col product grid right
   - Filter sidebar uses MOCK_PRODUCTS to compute category counts
   - Sort dropdown (Price: Low to High, High to Low, Newest)

2. **Product detail page** — `apps/storefront/app/shop/[slug]/page.tsx`
   - Breadcrumb: Home > Shop > {category} > {product name}
   - 2-col layout: large image left (60%), product details right (40%)
   - Right: BEST_SELLER badge, product name (h1), star rating + review count, price + originalPrice strikethrough + "Save ₱X" in coral
   - Short description paragraph
   - Color variant picker (buttons for each colorVariant)
   - "Inquire Now — Message Us" CTA (gold button, full width, MessageCircle icon)
   - Trust icons row: Free shipping / 30-day returns / 5-year warranty
   - Tabs: Description | Specifications | Reviews (static content for now)
   - "You May Also Like" section: 4-col grid of related products (same category)

3. **next.config.ts** — needs `images.remotePatterns` for placehold.co (images will 500 without it)

## GSD Phase 1 checkpoint
- User needs to visit http://localhost:3010/ui-preview and confirm brand colors render
- Once confirmed: type "approved" to the waiting executor agent (agentId: a70fdcfff6722f697)
- After that: verifier runs, roadmap updates, Phase 1 marked complete
</remaining_work>

<decisions_made>

- Tailwind v4 fix: `@source "../../../packages/ui/src/**/*.{ts,tsx}"` added to globals.css — Turbopack can't scan outside app directory automatically
- shadcn CSS variables (`--primary`, `--background`, `--ring`, etc.) added to globals.css base layer — shadcn primitives need these or render unstyled
- `design-system` route renamed to `ui-preview` — more descriptive for a developer token/component verification page
- Using `https://placehold.co/{W}x{H}/{bg-hex}/{text-hex}?text={label}` for all product images (no real assets yet)
- All brand token classes use `text-[--color-navy]` CSS custom property syntax (Tailwind v4 pattern)
- ProductCard uses `next/image` with `fill` + `aspect-[4/3]` wrapper
- Pages are Server Components — no "use client" needed for homepage or shop page
- Color variant picker on product detail will need "use client" for selected state
</decisions_made>

<blockers>

- **placehold.co images**: `next.config.ts` needs `images.remotePatterns` for placehold.co or Next.js image optimization will 500. Add before running dev server with the new pages.
- **Phase 1 checkpoint**: Still open. Executor agent a70fdcfff6722f697 is waiting for user's "approved" signal. Don't forget to close this out.
- **MOCK_PRODUCTS import path**: Uses `import { MOCK_PRODUCTS } from "@furnitrack/db"` (not `@furnitrack/db/mock`) — the db package re-exports everything from mock in its index.ts.
</blockers>

<context>
We were in the middle of building the storefront UI to match 3 reference designs:
1. Homepage (not logged in) — dark navy header/footer, beige bg, 2×2 hero grid, 4-col product sections
2. Shop page — filter sidebar left, 3-col product grid, hero banner
3. Product detail (item inquiring) — 2-col image+details, color picker, "Inquire Now" gold CTA, tabs, related products

The reference designs use Filipino peso (₱) pricing. Products come from MOCK_PRODUCTS (12 products, 6 categories).

The skill `~/.claude/skills/frontend-design.md` was created this session and should be consulted when resuming.

The homepage is fully built and should be visible at http://localhost:3010/ once the dev server restarts (it may still be running on port 3010 from the executor).

Next immediate actions when resuming:
1. Add placehold.co to next.config.ts images.remotePatterns
2. Build apps/storefront/app/shop/page.tsx
3. Build apps/storefront/app/shop/[slug]/page.tsx
4. Commit all uncommitted storefront work
5. Close Phase 1 checkpoint (get user to verify /ui-preview)
</context>

<next_action>
Start with: Add `images.remotePatterns` for placehold.co to `apps/storefront/next.config.ts`, then build `apps/storefront/app/shop/page.tsx`.
</next_action>
