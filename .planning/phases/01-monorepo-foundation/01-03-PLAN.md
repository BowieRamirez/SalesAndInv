---
phase: 01-monorepo-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/components.json
  - packages/ui/src/lib/utils.ts
  - packages/ui/src/components/ui/button.tsx
  - packages/ui/src/components/ui/badge.tsx
  - packages/ui/src/components/ui/card.tsx
  - packages/ui/src/components/ui/input.tsx
  - packages/ui/src/components/Button.tsx
  - packages/ui/src/components/Badge.tsx
  - packages/ui/src/components/Card.tsx
  - packages/ui/src/components/Input.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - FOUN-05
  - FOUN-06

must_haves:
  truths:
    - "Importing `Button` from `@furnitrack/ui` and rendering it with `variant='primary'` produces a gold-background button — TypeScript resolves the prop type, no import errors"
    - "The 4 FurniTrack wrapper components (Button, Badge, Card, Input) all re-export from @furnitrack/ui with correct prop interfaces"
    - "`packages/ui/src/index.ts` exports exactly Button, Badge, Card, Input — no raw shadcn primitives exported"
    - "shadcn/ui components.json exists at packages/ui/components.json (not repo root) — confirming the CLI ran with --cwd flag"
  artifacts:
    - path: "packages/ui/src/components/Button.tsx"
      provides: "FurniTrack Button with primary/secondary/ghost variants mapped to brand tokens"
      exports: ["Button", "FurniButtonProps"]
    - path: "packages/ui/src/components/Badge.tsx"
      provides: "FurniTrack Badge with BEST_SELLER/SALE/HOT/OUT_OF_STOCK/LOW_STOCK variants"
      exports: ["Badge", "FurniBadgeProps"]
    - path: "packages/ui/src/components/Card.tsx"
      provides: "FurniTrack Card wrapper with token-based shadow and padding"
      exports: ["Card", "FurniCardProps"]
    - path: "packages/ui/src/components/Input.tsx"
      provides: "FurniTrack Input with label and error state"
      exports: ["Input", "FurniInputProps"]
    - path: "packages/ui/src/index.ts"
      provides: "Public API of @furnitrack/ui — exports all 4 components"
      contains: "export { Button }"
    - path: "packages/ui/components.json"
      provides: "shadcn/ui CLI config for packages/ui — confirms --cwd flag was used"
      contains: "packages/ui"
  key_links:
    - from: "packages/ui/src/components/Button.tsx"
      to: "packages/ui/src/components/ui/button.tsx"
      via: "import { Button as ShadcnButton } from './ui/button'"
      pattern: "from.*./ui/button"
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/*.tsx"
      via: "named re-exports"
      pattern: "export.*from.*./components/"
---

<objective>
Initialize shadcn/ui inside `packages/ui` and build the 4 Phase 1 primitive components (Button, Badge, Card, Input) as FurniTrack wrappers over shadcn primitives.

Purpose: These 4 components are the entire Phase 1 UI deliverable. They verify that the design system is wired correctly — shared Tailwind tokens apply brand colors, shadcn accessibility behavior is preserved, and the `@furnitrack/ui` import path resolves in consuming apps. Every downstream phase builds on these patterns.

Output: A working `packages/ui` with shadcn initialized, 4 wrapped components with brand-aware variants, and a clean barrel export.
</objective>

<execution_context>
@C:/Users/bowie ramirez/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bowie ramirez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 — already created -->
<!-- packages/config/tailwind/tokens.css defines these CSS variables: -->
<!--   --color-navy, --color-coral, --color-gold, --color-beige, --color-charcoal, --color-muted, --color-white -->
<!-- packages/ui/package.json already exists with exports: { ".": "./src/index.ts" } -->
<!-- packages/ui/src/index.ts currently has `export {}` placeholder — REPLACE entirely -->

<!-- Design token Tailwind class names (Tailwind v4 generates utilities from @theme variables): -->
<!-- bg-navy, text-navy, bg-coral, text-coral, bg-gold, text-gold -->
<!-- bg-beige, text-beige, bg-charcoal, text-charcoal, text-muted -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui in packages/ui and install dependencies</name>
  <files>
    packages/ui/package.json,
    packages/ui/tsconfig.json,
    packages/ui/components.json,
    packages/ui/src/lib/utils.ts,
    packages/ui/src/components/ui/button.tsx,
    packages/ui/src/components/ui/badge.tsx,
    packages/ui/src/components/ui/card.tsx,
    packages/ui/src/components/ui/input.tsx
  </files>
  <action>
    Install dependencies first, then initialize shadcn/ui with the correct `--cwd` flag.

    **Step 1 — Install packages/ui dependencies:**
    ```bash
    pnpm add lucide-react --filter @furnitrack/ui
    pnpm add class-variance-authority clsx tailwind-merge --filter @furnitrack/ui
    ```

    **Step 2 — Add tsconfig to packages/ui:**

    **`packages/ui/tsconfig.json`**:
    ```json
    {
      "extends": "@furnitrack/config/typescript/base.json",
      "compilerOptions": {
        "outDir": "dist"
      },
      "include": ["src/**/*.ts", "src/**/*.tsx"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    **Step 3 — Initialize shadcn/ui inside packages/ui:**

    Run the shadcn init command FROM INSIDE `packages/ui` (critical — using --cwd avoids root-level pollution):
    ```bash
    cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem/packages/ui" && npx shadcn@latest init --yes
    ```

    If the interactive prompt cannot be avoided, use defaults:
    - Style: Default
    - Base color: Slate
    - CSS variables: Yes
    - Tailwind config: Yes

    Alternatively, if the CLI supports it: `npx shadcn@latest init --yes --cwd .` from packages/ui.

    **Step 4 — Add the 4 shadcn primitives:**
    ```bash
    cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem/packages/ui" && npx shadcn@latest add button badge card input --yes
    ```

    **After initialization — verify these files were created by shadcn CLI:**
    - `packages/ui/components.json` (shadcn config — must be at packages/ui level, NOT repo root)
    - `packages/ui/src/lib/utils.ts` (cn utility function)
    - `packages/ui/src/components/ui/button.tsx`
    - `packages/ui/src/components/ui/badge.tsx`
    - `packages/ui/src/components/ui/card.tsx`
    - `packages/ui/src/components/ui/input.tsx`

    If shadcn places files at a different path (e.g., `packages/ui/components/ui/`), hand-write the `src/lib/utils.ts` and the 4 component files manually using the patterns from RESEARCH.md:

    **`packages/ui/src/lib/utils.ts`** (if CLI did not create it):
    ```typescript
    import { clsx, type ClassValue } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    CRITICAL: Confirm `components.json` exists at `packages/ui/components.json`, NOT at the repo root. If it appears at the repo root, move it and the generated components to `packages/ui/`.
  </action>
  <verify>
    <automated>cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem" &amp;&amp; ls packages/ui/components.json packages/ui/src/lib/utils.ts packages/ui/src/components/ui/button.tsx 2&gt;&amp;1</automated>
  </verify>
  <done>`packages/ui/components.json` exists (not at repo root). `packages/ui/src/lib/utils.ts` exists with `cn` function. All 4 shadcn primitive files exist in `packages/ui/src/components/ui/`.</done>
</task>

<task type="auto">
  <name>Task 2: Build 4 FurniTrack wrapper components and wire the barrel export</name>
  <files>
    packages/ui/src/components/Button.tsx,
    packages/ui/src/components/Badge.tsx,
    packages/ui/src/components/Card.tsx,
    packages/ui/src/components/Input.tsx,
    packages/ui/src/index.ts
  </files>
  <action>
    Create 4 FurniTrack-specific wrapper components. Each wraps the corresponding shadcn primitive and applies brand token classes as defaults. Consuming code imports from `@furnitrack/ui` only — never from the shadcn `ui/` subdirectory.

    **`packages/ui/src/components/Button.tsx`**:
    ```typescript
    import { Button as ShadcnButton } from "./ui/button"
    import type { ButtonProps } from "./ui/button"
    import { cn } from "../lib/utils"

    export interface FurniButtonProps extends Omit<ButtonProps, "variant"> {
      variant?: "primary" | "secondary" | "ghost"
    }

    export function Button({ variant = "primary", className, ...props }: FurniButtonProps) {
      const variantClasses: Record<string, string> = {
        primary: "bg-gold text-white hover:bg-gold/90 border-transparent",
        secondary: "bg-navy text-white hover:bg-navy/90 border-transparent",
        ghost: "bg-transparent text-charcoal hover:bg-beige border-transparent",
      }
      return (
        <ShadcnButton
          variant="default"
          className={cn(variantClasses[variant], className)}
          {...props}
        />
      )
    }
    ```

    **`packages/ui/src/components/Badge.tsx`**:
    ```typescript
    import { Badge as ShadcnBadge } from "./ui/badge"
    import type { BadgeProps } from "./ui/badge"
    import { cn } from "../lib/utils"

    export type FurniBadgeVariant =
      | "BEST_SELLER"
      | "SALE"
      | "HOT"
      | "OUT_OF_STOCK"
      | "LOW_STOCK"
      | "default"

    export interface FurniBadgeProps extends Omit<BadgeProps, "variant"> {
      variant?: FurniBadgeVariant
    }

    const badgeClasses: Record<FurniBadgeVariant, string> = {
      BEST_SELLER: "bg-gold text-white",
      SALE: "bg-coral text-white",
      HOT: "bg-coral text-white",
      OUT_OF_STOCK: "bg-charcoal text-white",
      LOW_STOCK: "bg-gold/20 text-charcoal border-gold/40",
      default: "bg-muted/20 text-charcoal",
    }

    const badgeLabels: Record<FurniBadgeVariant, string> = {
      BEST_SELLER: "Best Seller",
      SALE: "Sale",
      HOT: "Hot",
      OUT_OF_STOCK: "Out of Stock",
      LOW_STOCK: "Low Stock",
      default: "",
    }

    export function Badge({
      variant = "default",
      className,
      children,
      ...props
    }: FurniBadgeProps) {
      return (
        <ShadcnBadge
          variant="outline"
          className={cn(badgeClasses[variant], className)}
          {...props}
        >
          {children ?? badgeLabels[variant]}
        </ShadcnBadge>
      )
    }
    ```

    **`packages/ui/src/components/Card.tsx`**:
    ```typescript
    import {
      Card as ShadcnCard,
      CardHeader,
      CardContent,
      CardFooter,
      CardTitle,
      CardDescription,
    } from "./ui/card"
    import type { HTMLAttributes } from "react"
    import { cn } from "../lib/utils"

    export interface FurniCardProps extends HTMLAttributes<HTMLDivElement> {
      padding?: "sm" | "md" | "lg"
    }

    const paddingClasses = {
      sm: "p-3",
      md: "p-5",
      lg: "p-8",
    }

    export function Card({ padding = "md", className, children, ...props }: FurniCardProps) {
      return (
        <ShadcnCard
          className={cn(
            "bg-white shadow-sm border border-beige rounded-lg",
            paddingClasses[padding],
            className
          )}
          {...props}
        >
          {children}
        </ShadcnCard>
      )
    }

    // Re-export shadcn Card sub-components for use with FurniTrack Card
    export { CardHeader, CardContent, CardFooter, CardTitle, CardDescription }
    ```

    **`packages/ui/src/components/Input.tsx`**:
    ```typescript
    import { Input as ShadcnInput } from "./ui/input"
    import type { InputProps } from "./ui/input"
    import { cn } from "../lib/utils"

    export interface FurniInputProps extends InputProps {
      label?: string
      error?: string
    }

    export function Input({ label, error, className, id, ...props }: FurniInputProps) {
      const inputId = id ?? label?.toLowerCase().replace(/\s+/g, "-")
      return (
        <div className="flex flex-col gap-1">
          {label && (
            <label htmlFor={inputId} className="text-sm font-medium text-charcoal">
              {label}
            </label>
          )}
          <ShadcnInput
            id={inputId}
            className={cn(
              "border-muted/40 focus:border-navy focus:ring-1 focus:ring-navy",
              error && "border-coral focus:border-coral focus:ring-coral",
              className
            )}
            aria-invalid={!!error}
            aria-describedby={error ? `${inputId}-error` : undefined}
            {...props}
          />
          {error && (
            <span id={`${inputId}-error`} className="text-sm text-coral">
              {error}
            </span>
          )}
        </div>
      )
    }
    ```

    **`packages/ui/src/index.ts`** — single barrel, replaces `export {}` placeholder:
    ```typescript
    export { Button } from "./components/Button"
    export { Badge } from "./components/Badge"
    export { Card, CardHeader, CardContent, CardFooter, CardTitle, CardDescription } from "./components/Card"
    export { Input } from "./components/Input"

    export type { FurniButtonProps } from "./components/Button"
    export type { FurniBadgeProps, FurniBadgeVariant } from "./components/Badge"
    export type { FurniCardProps } from "./components/Card"
    export type { FurniInputProps } from "./components/Input"
    ```

    IMPORTANT: Do NOT export any raw shadcn primitives from `src/index.ts`. Consuming code must never import from `@furnitrack/ui/src/components/ui/*` directly.
  </action>
  <verify>
    <automated>cd "C:/Users/bowie ramirez/OneDrive/Desktop/Infoman/SalesInvSystem" &amp;&amp; pnpm --filter @furnitrack/ui typecheck 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>`pnpm --filter @furnitrack/ui typecheck` exits 0. `packages/ui/src/index.ts` exports Button, Badge, Card, and Input. No TypeScript errors in any component file.</done>
</task>

</tasks>

<verification>
Run `pnpm turbo typecheck --filter=@furnitrack/ui` from repo root — exits 0. Confirm `packages/ui/components.json` is at the package level (not repo root). Confirm `packages/ui/src/index.ts` does NOT re-export any raw shadcn paths.
</verification>

<success_criteria>
- `packages/ui/components.json` exists at `packages/ui/` level
- `packages/ui/src/lib/utils.ts` contains the `cn` helper function
- All 4 shadcn primitives exist in `packages/ui/src/components/ui/`
- All 4 FurniTrack wrapper components exist in `packages/ui/src/components/`
- `pnpm --filter @furnitrack/ui typecheck` exits 0
- FOUN-05 (Phase 1 scope): Button, Badge, Card, Input exported from @furnitrack/ui — confirmed
- FOUN-06: Design token class names (bg-gold, bg-navy, text-coral, bg-beige) used in wrapper components — confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-03-SUMMARY.md` using the standard summary template.
</output>
